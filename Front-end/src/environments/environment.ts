// Exporta un objeto llamado 'environment' que contiene configuraciones específicas para el entorno de desarrollo
export const environment = {
  // Indica que la aplicación no está en modo de producción
  production: false,
  
  // Define la URL base del endpoint de la API que la aplicación utilizará durante el desarrollo
  endpoint: "http://localhost:5208/api/"
};