// Exporta un objeto llamado 'environment' que contiene configuraciones para la aplicación
export const environment = {
  // Indica si la aplicación está en modo de producción
  production: true,
  
  // Define la URL base del endpoint de la API que la aplicación utilizará
  endpoint: "http://localhost:5208/api/"
};