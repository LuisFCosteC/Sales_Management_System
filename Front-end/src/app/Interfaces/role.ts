export interface Role {                     // Exporta una interfaz llamada 'Role' que define la estructura de un objeto de rol
    idRole: number,                        // Propiedad que representa el identificador único del rol
    name: string                           // Propiedad que representa el nombre del rol
}